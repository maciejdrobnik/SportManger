import 'package:flutter/material.dart';

class ChoresDetails extends StatelessWidget {
  const ChoresDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
